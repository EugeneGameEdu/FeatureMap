# FeatureMap MVP — Техническая спецификация

> Документ для разработки с помощью Codex. Описывает что делать, как и в каком порядке.

---

## 🎯 Цель MVP

Создать инструмент который:
1. **Алгоритмически** сканирует TypeScript/JavaScript проект и строит граф зависимостей
2. **Через AI** превращает граф в человекопонятную карту фич
3. **Визуализирует** карту в веб-интерфейсе с React Flow
4. **Автоматически** обновляет данные при изменениях в коде

**Тестовый проект:** сам FeatureMap (уроборос)

---

## 📦 Структура репозитория

```
featuremap/
├── packages/
│   ├── cli/                    # CLI инструмент + алгоритмический анализ
│   │   ├── src/
│   │   │   ├── commands/
│   │   │   │   ├── init.ts     # featuremap init
│   │   │   │   ├── scan.ts     # featuremap scan
│   │   │   │   └── web.ts      # featuremap web
│   │   │   ├── analyzer/
│   │   │   │   ├── parser.ts   # ts-morph парсинг
│   │   │   │   ├── graph.ts    # построение графа зависимостей
│   │   │   │   └── grouper.ts  # группировка по папкам
│   │   │   ├── watcher/
│   │   │   │   └── index.ts    # отслеживание изменений
│   │   │   └── index.ts        # точка входа CLI
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── mcp-server/             # MCP сервер для AI
│   │   ├── src/
│   │   │   ├── tools/
│   │   │   │   ├── getStructure.ts
│   │   │   │   ├── analyzeFeatures.ts
│   │   │   │   └── saveFeatureMap.ts
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── web/                    # React приложение
│       ├── src/
│       │   ├── components/
│       │   │   ├── FeatureMap.tsx      # React Flow карта
│       │   │   ├── FeatureNode.tsx     # Кастомная нода
│       │   │   ├── Sidebar.tsx         # Детали фичи
│       │   │   └── Header.tsx
│       │   ├── lib/
│       │   │   └── loadFeatureMap.ts   # Загрузка YAML
│       │   ├── App.tsx
│       │   └── main.tsx
│       ├── package.json
│       └── vite.config.ts
│
├── .featuremap/                # Данные (создаётся при init)
│   ├── config.yaml
│   ├── raw-graph.yaml          # Сырые данные от алгоритмов
│   ├── features/
│   │   └── *.yaml              # Фичи (после AI-анализа)
│   └── graph.yaml              # Связи для React Flow
│
├── package.json                # Корневой (workspaces)
├── tsconfig.json               # Базовый конфиг TS
└── README.md
```

---

## 🔧 Технологический стек

### CLI + Analyzer
```json
{
  "dependencies": {
    "ts-morph": "^21.0.0",
    "fast-glob": "^3.3.0",
    "yaml": "^2.3.0",
    "commander": "^12.0.0",
    "chokidar": "^3.5.0"
  }
}
```

### MCP Server
```json
{
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "yaml": "^2.3.0"
  }
}
```

### Web
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "@xyflow/react": "^12.0.0",
    "js-yaml": "^4.1.0",
    "lucide-react": "^0.300.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
```

**shadcn/ui компоненты** (копируются, не устанавливаются):
- Button
- Card
- Badge
- ScrollArea
- Sheet (для сайдбара)

---

## 📄 Форматы данных

### config.yaml
```yaml
# .featuremap/config.yaml
version: 1

project:
  name: "featuremap"
  root: "."

scan:
  include:
    - "packages/*/src/**/*.ts"
    - "packages/*/src/**/*.tsx"
  exclude:
    - "**/*.test.ts"
    - "**/*.spec.ts"
    - "**/node_modules/**"

features:
  # Подсказки для группировки (опционально)
  hints:
    - pattern: "packages/cli/src/commands/*"
      type: "command"
    - pattern: "packages/web/src/components/*"
      type: "ui-component"
```

### raw-graph.yaml (генерируется алгоритмами)
```yaml
# .featuremap/raw-graph.yaml
version: 1
generatedAt: "2024-12-13T10:00:00Z"

files:
  "packages/cli/src/commands/init.ts":
    exports:
      - name: "initCommand"
        type: "function"
    imports:
      internal:
        - "../analyzer/parser"
        - "../analyzer/graph"
      external:
        - "commander"
        - "yaml"
    linesOfCode: 45

  "packages/cli/src/analyzer/parser.ts":
    exports:
      - name: "parseProject"
        type: "function"
      - name: "parseFile"
        type: "function"
    imports:
      internal: []
      external:
        - "ts-morph"
        - "fast-glob"
    linesOfCode: 120

  # ... остальные файлы

# Граф зависимостей (кто кого импортирует)
dependencies:
  "packages/cli/src/commands/init.ts":
    - "packages/cli/src/analyzer/parser.ts"
    - "packages/cli/src/analyzer/graph.ts"
  "packages/cli/src/commands/scan.ts":
    - "packages/cli/src/analyzer/parser.ts"
    - "packages/cli/src/analyzer/graph.ts"
    - "packages/cli/src/analyzer/grouper.ts"
  # ...

# Кластеры (сильно связанные файлы)
clusters:
  - id: "cli-commands"
    files:
      - "packages/cli/src/commands/init.ts"
      - "packages/cli/src/commands/scan.ts"
      - "packages/cli/src/commands/web.ts"
  - id: "cli-analyzer"
    files:
      - "packages/cli/src/analyzer/parser.ts"
      - "packages/cli/src/analyzer/graph.ts"
      - "packages/cli/src/analyzer/grouper.ts"
  # ...
```

### features/*.yaml (после AI-анализа)
```yaml
# .featuremap/features/cli-commands.yaml
id: "cli-commands"
name: "CLI Commands"
description: "Command-line interface for initializing, scanning, and running the web viewer"
source: ai  # или "auto" если создано алгоритмически
status: active

files:
  - path: "packages/cli/src/commands/init.ts"
    role: "Initializes .featuremap directory and config"
  - path: "packages/cli/src/commands/scan.ts"
    role: "Runs project analysis"
  - path: "packages/cli/src/commands/web.ts"
    role: "Starts web server"

exports:
  - initCommand
  - scanCommand
  - webCommand

dependsOn:
  - "cli-analyzer"  # ссылка на другую фичу

metadata:
  createdAt: "2024-12-13T10:00:00Z"
  updatedAt: "2024-12-13T10:00:00Z"
```

### graph.yaml (для React Flow)
```yaml
# .featuremap/graph.yaml
version: 1
generatedAt: "2024-12-13T10:00:00Z"

nodes:
  - id: "cli-commands"
    label: "CLI Commands"
    type: "feature"
    fileCount: 3
    position:  # опционально, React Flow может автолейаут
      x: 0
      y: 0

  - id: "cli-analyzer"
    label: "Code Analyzer"
    type: "feature"
    fileCount: 3
    position:
      x: 200
      y: 100

  - id: "mcp-server"
    label: "MCP Server"
    type: "feature"
    fileCount: 4

  - id: "web-ui"
    label: "Web Interface"
    type: "feature"
    fileCount: 5

edges:
  - source: "cli-commands"
    target: "cli-analyzer"
  - source: "mcp-server"
    target: "cli-analyzer"
```

---

## 🛠 CLI команды

### `featuremap init`

```bash
npx featuremap init
```

**Что делает:**
1. Создаёт `.featuremap/` директорию
2. Создаёт `config.yaml` с дефолтными настройками
3. Добавляет `.featuremap/raw-graph.yaml` в `.gitignore` (кэш)
4. Настраивает husky hook для автообновления

**Вывод:**
```
✓ Created .featuremap/config.yaml
✓ Created .featuremap/features/
✓ Added husky pre-commit hook
✓ Ready! Run 'featuremap scan' to analyze your project
```

### `featuremap scan`

```bash
npx featuremap scan [--ai]
```

**Без флага --ai:**
1. Парсит все файлы по паттернам из config
2. Строит граф зависимостей
3. Группирует по папкам (эвристика)
4. Сохраняет `raw-graph.yaml`
5. Создаёт черновые фичи в `features/` с `source: auto`
6. Генерирует `graph.yaml`

**С флагом --ai:**
1. Всё то же самое
2. Плюс запускает MCP сервер
3. Ждёт что AI (Codex) переименует/опишет фичи
4. Обновляет `features/*.yaml` с `source: ai`

**Вывод:**
```
Scanning project...
  ✓ Found 15 files
  ✓ Built dependency graph
  ✓ Identified 4 feature clusters

Features found:
  • cli-commands (3 files)
  • cli-analyzer (3 files)
  • mcp-server (4 files)
  • web-ui (5 files)

✓ Saved to .featuremap/
```

### `featuremap web`

```bash
npx featuremap web [--port 3000]
```

**Что делает:**
1. Запускает Vite dev server с веб-приложением
2. Веб читает `.featuremap/graph.yaml` и `features/*.yaml`
3. Показывает карту

**Вывод:**
```
Starting FeatureMap web interface...
  ➜ Local: http://localhost:3000/
  ➜ Press Ctrl+C to stop
```

---

## 🤖 MCP Server — Tools

### Tool: `get_project_structure`

**Описание:** Возвращает сырой граф зависимостей проекта

**Параметры:** нет

**Возвращает:**
```json
{
  "files": { ... },
  "dependencies": { ... },
  "clusters": [ ... ]
}
```

**Когда использовать:** AI хочет понять структуру проекта

### Tool: `get_current_features`

**Описание:** Возвращает текущую карту фич

**Параметры:** нет

**Возвращает:**
```json
{
  "features": [
    {
      "id": "cli-commands",
      "name": "cli-commands",
      "description": null,
      "source": "auto",
      "files": [...]
    }
  ]
}
```

**Когда использовать:** AI хочет увидеть что уже определено

### Tool: `update_feature`

**Описание:** Обновляет информацию о фиче

**Параметры:**
```json
{
  "id": "cli-commands",
  "name": "CLI Commands",
  "description": "Command-line interface for FeatureMap"
}
```

**Возвращает:**
```json
{
  "success": true,
  "feature": { ... }
}
```

**Когда использовать:** AI хочет переименовать или описать фичу

### Tool: `merge_features`

**Описание:** Объединяет несколько фич в одну

**Параметры:**
```json
{
  "sourceIds": ["feature-1", "feature-2"],
  "targetId": "combined-feature",
  "name": "Combined Feature",
  "description": "..."
}
```

### Tool: `split_feature`

**Описание:** Разделяет фичу на несколько

**Параметры:**
```json
{
  "sourceId": "big-feature",
  "splits": [
    { "id": "part-1", "files": ["file1.ts", "file2.ts"] },
    { "id": "part-2", "files": ["file3.ts"] }
  ]
}
```

---

## 🌐 Веб-интерфейс

### Макет

```
┌─────────────────────────────────────────────────────────────────┐
│  FeatureMap                              [Refresh] [Settings ⚙]  │
│  featuremap • 4 features • updated 2 min ago                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    ┌─────────────────┐                          │
│                    │  CLI Commands   │                          │
│                    │    3 files      │                          │
│                    └────────┬────────┘                          │
│                             │                                   │
│                             ▼                                   │
│         ┌─────────────────────────────────────┐                 │
│         │          Code Analyzer              │                 │
│         │            3 files                  │                 │
│         └─────────────────────────────────────┘                 │
│                    ▲                 ▲                          │
│                    │                 │                          │
│         ┌─────────────────┐  ┌─────────────────┐                │
│         │   MCP Server    │  │   Web Interface │                │
│         │    4 files      │  │     5 files     │                │
│         └─────────────────┘  └─────────────────┘                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### При клике на ноду — сайдбар

```
┌─────────────────────────────────────────────────┐
│  CLI Commands                              [×]  │
├─────────────────────────────────────────────────┤
│                                                 │
│  Command-line interface for initializing,       │
│  scanning, and running the web viewer           │
│                                                 │
│  ─────────────────────────────────────────────  │
│                                                 │
│  FILES                                          │
│  ├── commands/init.ts                           │
│  ├── commands/scan.ts                           │
│  └── commands/web.ts                            │
│                                                 │
│  ─────────────────────────────────────────────  │
│                                                 │
│  EXPORTS                                        │
│  initCommand • scanCommand • webCommand         │
│                                                 │
│  ─────────────────────────────────────────────  │
│                                                 │
│  DEPENDS ON                                     │
│  → Code Analyzer                                │
│                                                 │
│  ─────────────────────────────────────────────  │
│                                                 │
│  ◉ ai-analyzed                                  │
│  Updated 2 min ago                              │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Компоненты

**FeatureNode.tsx** — кастомная нода для React Flow:
- Название фичи
- Количество файлов
- Бейдж статуса (ai/auto)
- Цвет по типу (если будет)

**Sidebar.tsx** — детали фичи:
- Sheet из shadcn (выезжает справа)
- Описание
- Список файлов
- Экспорты
- Зависимости

**Header.tsx:**
- Название проекта
- Кнопка Refresh (перечитывает YAML)
- Время последнего обновления

---

## 🔄 Автообновление (Husky)

### Что устанавливает `featuremap init`:

```json
// package.json (проекта пользователя)
{
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
```

```bash
# .husky/pre-commit
npx featuremap scan --quick
```

### `featuremap scan --quick`

Быстрый режим:
- Смотрит git diff (какие файлы изменились)
- Обновляет только затронутые части `raw-graph.yaml`
- Обновляет `graph.yaml`
- НЕ вызывает AI

Полный `scan` (без --quick) — пересканирует всё.

---

## 📋 План реализации

### Фаза 1: Скелет (день 1)

- [ ] Инициализировать monorepo (npm workspaces)
- [ ] Настроить TypeScript конфиги
- [ ] Создать базовую структуру всех трёх пакетов
- [ ] CLI: команда `init` которая создаёт папку и конфиг

### Фаза 2: Анализатор (день 2-3)

- [ ] parser.ts — парсинг файлов через ts-morph
- [ ] graph.ts — построение графа зависимостей
- [ ] grouper.ts — группировка по папкам
- [ ] Команда `scan` — собирает всё вместе
- [ ] Генерация `raw-graph.yaml` и `graph.yaml`

### Фаза 3: Веб-интерфейс (день 3-4)

- [ ] Настроить Vite + React + Tailwind
- [ ] Установить shadcn компоненты
- [ ] FeatureMap.tsx с React Flow
- [ ] FeatureNode.tsx — кастомная нода
- [ ] Sidebar.tsx — детали фичи
- [ ] Загрузка и парсинг YAML
- [ ] Команда `web`

### Фаза 4: MCP Server (день 4-5)

- [ ] Базовый MCP сервер
- [ ] Tool: get_project_structure
- [ ] Tool: get_current_features
- [ ] Tool: update_feature
- [ ] Интеграция с CLI (`scan --ai`)

### Фаза 5: Автообновление (день 5)

- [ ] Husky интеграция в `init`
- [ ] Режим `scan --quick`
- [ ] Тестирование на самом проекте

### Фаза 6: Полировка (день 6)

- [ ] Уроборос: проанализировать сам FeatureMap
- [ ] Исправить баги
- [ ] README с инструкциями
- [ ] Скриншоты/гифки для демо

---

## 🐍 Уроборос: ожидаемый результат

После завершения MVP, при запуске на самом себе, должны получить примерно такие фичи:

| ID | Name | Files | Description |
|----|------|-------|-------------|
| cli-commands | CLI Commands | 3 | Команды init, scan, web |
| cli-analyzer | Code Analyzer | 3 | Парсинг и анализ кода |
| cli-watcher | File Watcher | 1 | Отслеживание изменений |
| mcp-tools | MCP Tools | 4 | Инструменты для AI |
| web-components | Web UI | 5 | React компоненты карты |
| web-lib | Web Utilities | 1 | Загрузка данных |

Связи:
- cli-commands → cli-analyzer
- cli-commands → cli-watcher
- mcp-tools → cli-analyzer
- web-components → web-lib

---

## ⚠️ Известные ограничения MVP

1. **Только TypeScript/JavaScript** — другие языки позже
2. **Локально** — нет облака, всё на машине пользователя
3. **Один проект** — нет мульти-репо поддержки
4. **Простой лейаут** — React Flow dagre, без ручного позиционирования
5. **Нет истории** — только текущее состояние

---

## 🚀 Команда для старта

```bash
# Создать репозиторий
mkdir featuremap && cd featuremap
git init

# Инициализировать
npm init -y

# Дальше — Codex
```

---

*Документ готов для разработки. Обновлять по мере прогресса.*
