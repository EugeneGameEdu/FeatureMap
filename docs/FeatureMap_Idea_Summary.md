# FeatureMap — Карта проекта, понятная всей команде

> Документ-саммари по итогам мозгового штурма. Не техническая спецификация, а живое описание идеи.

---

## 🎯 Суть в одном абзаце

**FeatureMap** — это визуальная карта фич проекта, которая автоматически строится из кода и даёт общий язык для всей команды. Разработчик кликает на фичу — попадает в код. Дизайнер видит что есть и как связано. PM понимает реальное состояние проекта. AI помогает поддерживать актуальность и отвечает на вопросы о проекте.

Это **мост между кодом и людьми**, которые код не пишут.

---

## 😫 Проблема, которую решаем

### Вечная боль команд:

```
Дизайнер: "У нас есть система квестов?"
Разработчик: "Ну типа да, но там не всё..."
Дизайнер: "А что именно есть?"
Разработчик: *вздыхает, открывает код, 30 минут объясняет*
```

### Почему существующие инструменты не решают:

| Инструмент | Что не так |
|------------|------------|
| **Jira/Linear** | Задачи, не фичи. Нет связи с кодом. Все ненавидят заполнять |
| **Confluence/Notion** | Документация устаревает на следующий день |
| **Storybook** | UI-компоненты, не фичи. Только для фронтенда |
| **CodeSee** | Для технарей. Дизайнер откроет и закроет |

### Ключевой инсайт:

Все в команде говорят о **фичах**. Но нет инструмента, который показывает фичи как единицу — со связями, статусом, и привязкой к коду.

### Почему раньше никто не сделал:

1. **До AI было невозможно** — фича это человеческая абстракция, алгоритмами не вычислить
2. **Проблема на стыке дисциплин** — разработчики не видят боль дизайнеров, дизайнеры не могут в код
3. **"И так работает"** — команды терпят созвоны и объяснения, пока боль не станет невыносимой
4. **Timing** — AI дозрел буквально год-два назад (GPT-4, Claude)

---

## 👥 Для кого это

### Основные аудитории:

**1. Веб-команды (5-50 человек)**
- Есть разрыв между технарями и не-технарями
- Проекты сложные (десятки и сотни фич)
- Есть бюджет на инструменты

**2. Геймдев-студии**
- Геймдизайнеры не программисты, но им надо понимать что возможно
- Игра = паутина связанных систем
- Документация не успевает за изменениями

**3. Вайбкодеры**
- Напромптили проект, потерялись в нём
- Нужна карта чтобы не сломать всё
- Переключаются между проектами, забывают контекст

### Кто использует как:

| Роль | Как использует | Где |
|------|----------------|-----|
| Разработчик | Работает в редакторе, AI сам использует MCP | Cursor / VS Code |
| Дизайнер | Смотрит карту, задаёт вопросы | Веб-редактор |
| PM | Видит реальный прогресс проекта | Веб-редактор |
| Новичок | За час понимает структуру проекта | Веб-редактор |

---

## 💡 Киллер-фича: AI как технический советник

Это не просто карта. Это **AI, который понимает твой проект**.

### Сценарии:

**Геймдизайнер:**
> "Хочу добавить систему питомцев"
> 
> AI: "Это затронет Inventory, Combat, UI. Связано с 5 существующими фичами. Сложность высокая."

**PM:**
> "Можем вырезать систему кланов к релизу?"
> 
> AI: "Кланы связаны только с Chat и Leaderboards. Риск низкий, эти фичи продолжат работать."

**Разработчик:**
> "Хочу переписать авторизацию. Что сломается?"
> 
> AI: "Auth используется в 4 местах. Критичная связь — Payments. Начни с тестов там."

**Новичок:**
> "Объясни архитектуру проекта"
> 
> AI: "15 основных фич. Ядро — Player и Inventory. От них зависит всё остальное. Вот карта..."

### Почему это работает:

AI не читает весь код (проекты огромные). Он читает **карту** — структуру, связи, описания. Этого достаточно чтобы давать полезные советы.

Это не замена разработчику. Это **первичный фильтр**, чтобы дизайнер пришёл к разрабу с осмысленным запросом, а не с "хочу кнопку".

---

## 🏗️ Архитектура: MCP сервер + Веб

### Почему MCP, а не VS Code плагин:

**MCP (Model Context Protocol)** — универсальный протокол для AI-инструментов. Работает везде:
- Cursor
- VS Code + Continue
- Claude Desktop
- Windsurf
- Любой клиент с поддержкой MCP

**Один сервер → все платформы.** Не надо делать отдельный плагин для каждого редактора.

### Общая схема:

```
┌─────────────────────────────────────────────────────┐
│                    MCP сервер                       │
│                  (featuremap)                       │
│                                                     │
│  Запуск: npx featuremap serve                       │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ File Watcher (автоматический)               │    │
│  │ • Следит за изменениями файлов              │    │
│  │ • Обновляет карту автоматически             │    │
│  │ • Не требует действий от пользователя       │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ MCP интерфейс (для AI)                      │    │
│  │ • scan_project — структура проекта          │    │
│  │ • analyze_folder — анализ папки             │    │
│  │ • get_feature_map — текущая карта           │    │
│  │ • update_feature — обновить фичу            │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ HTTP + WebSocket (для веба)                 │    │
│  │ • localhost:3000 — веб-интерфейс            │    │
│  │ • Live updates через WebSocket              │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │  feature-map.yaml   │
              │  (в репозитории)    │
              │                     │
              │  • Источник правды  │
              │  • Версионируется   │
              │  • Читаемый людьми  │
              └─────────────────────┘
```

### Кто кого вызывает:

```
Пользователь: "Построй карту фич"
      │
      ▼
AI (Cursor/Claude) думает:
"Есть MCP сервер featuremap, использую его"
      │
      ▼
Вызывает: featuremap.scan_project()
      │
      ▼
MCP сервер отвечает: { features: [...] }
      │
      ▼
AI показывает результат пользователю
```

**Важно:** Твой сервер ничего не просит. Он просто говорит "вот что я умею", а AI сам решает когда использовать.

### Автоматическое обновление (главная фича):

```
Сервер запущен (npx featuremap serve)
         │
         │ следит за файлами (File Watcher)
         ▼
Разработчик сохранил файл
         │
         ▼
Сервер: "О, src/auth/login.ts изменился"
         │
         ▼
Перепарсил файл (быстро, без AI)
         │
         ▼
Обновил feature-map.yaml
         │
         ▼
WebSocket → Веб: "Карта обновилась"
         │
         ▼
Веб перерисовал мгновенно
```

**Пользователь ничего не делает.** Запустил сервер один раз — дальше всё автоматически.

### Что отслеживаем:

**Только main/prod ветку** — карта показывает "что есть в продукте", не "над чем работают".

```
Ветки разработчиков:          Карта показывает:

feature/auth (Вася)     ─┐
feature/quests (Петя)   ─┤     Только то, что
feature/shop (Маша)     ─┼──►  уже в main/prod
bugfix/login (Коля)     ─┤     
feature/chat (Аня)      ─┘     (готовые фичи)
```

Для задач в работе есть Jira/Linear. FeatureMap показывает результат, не процесс.

---

## 🌐 Веб-интерфейс

### Что в вебе:

```
┌─────────────────────────────────────────────────────┐
│  FeatureMap              [Settings ⚙️] [AI Chat 💬] │
├─────────────────────────────────────────────────────┤
│                                                     │
│         ┌─────┐         ┌─────┐                     │
│         │Auth │────────►│Users│                     │
│         └─────┘         └─────┘                     │
│              │               │                      │
│              ▼               ▼                      │
│         ┌─────┐         ┌─────┐                     │
│         │Mail │         │Shop │                     │
│         └─────┘         └─────┘                     │
│                                                     │
│  React Flow карта с нодами и связями                │
│  Клик на фичу → детали, файлы, описание             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### Настройки в вебе:

```
┌─────────────────────────────────────────────────────┐
│  Настройки проекта                                  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Ветка для отслеживания: [main ▼]                   │
│                                                     │
│  Анализировать:                                     │
│  ☑ src/features                                     │
│  ☑ src/modules                                      │
│  ☐ src/utils (утилиты, не фичи)                    │
│  ☐ src/legacy (старый код)                         │
│                                                     │
│  [Сканировать заново]                               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### AI в вебе:

**Проблема:** Pro подписка на Cursor ≠ API ключ. Это разные вещи.

```
Cursor Pro ($20/мес):           API ключ (отдельно):

Работает внутри Cursor          Нужно купить отдельно
Всё включено                    Платишь за токены
```

**Решение для MVP:**

| Где | AI | Как |
|-----|-----|-----|
| Cursor/VS Code | Работает (их подписка) | Через MCP |
| Веб | Без AI или опционально | Свой ключ в настройках |

В вебе AI опционален. Карта работает и без него.

---

## 📁 Формат данных

### feature-map.yaml:

```yaml
project:
  name: "My Awesome Game"
  description: "RPG с открытым миром"

features:
  quest-system:
    description: "Система квестов и заданий"
    status: active
    source: auto          # AI нашёл
    symbols:
      - src/quests/QuestManager.cs::QuestManager
      - src/quests/QuestLog.cs::QuestLog
    depends_on: [inventory, rewards, dialogue]
    complexity: high
    
  inventory:
    description: "Инвентарь игрока"
    status: active
    source: manual        # человек создал
    symbols:
      - src/player/Inventory.cs::Inventory
      - src/items/ItemDatabase.cs::ItemDatabase
    depends_on: [database]
    complexity: medium

  old-tutorial:
    status: ignored       # команда решила не трекать
    source: auto
    reason: "Legacy, будем удалять"
```

### Почему символы, а не строки:

```yaml
# Плохо (строки уедут при редактировании):
files:
  - src/quests/create.ts:15-45

# Хорошо (функции редко переименовываются):
symbols:
  - src/quests/create.ts::createQuest
  - src/quests/create.ts::QuestValidator
```

### Конфиг проекта (featuremap.config.yaml):

```yaml
# Какую ветку отслеживать
tracking:
  branch: main

# Что игнорировать
ignore:
  paths:
    - "src/legacy/*"
    - "**/*.test.ts"
  patterns:
    - "*Mock*"
    - "*Deprecated*"

# Подсказки для AI
hints:
  - "Папка src/modules/ — каждая подпапка это фича"
  - "Файлы .api.ts — это бэкенд-связь"

# AI настройки (опционально)
ai:
  provider: openai      # или anthropic
  api_key: ${OPENAI_KEY}  # из переменной окружения
```

---

## 📦 Дистрибуция

### Установка для разработчика:

```bash
# Запустить в проекте
npx featuremap serve

# Или с инициализацией
npx featuremap init
```

### Подключение к AI-клиенту:

```json
// Cursor: ~/.cursor/mcp.json
{
  "mcpServers": {
    "featuremap": {
      "command": "npx",
      "args": ["featuremap-mcp"]
    }
  }
}
```

Всё. Одна строка в конфиге.

### MCP в маркетплейсе:

MCP серверы можно публиковать в маркетплейсах (например, в VS Code). Пользователь видит:

```
FeatureMap                    ⭐ 1.2K
─────────────────────────────────────
Visual feature map for your codebase

[Install]  [Install in Workspace]

▸ Install in Cursor
▸ Install in Claude Code  
▸ Install in VS Code
```

Кликнул — установилось.

### Один сервер — все языки:

```
MCP сервер
    │
    ├── .ts/.js файлы → TypeScript парсер
    ├── .cs файлы → C# парсер (Unity)
    ├── .cpp файлы → C++ парсер (Unreal)
    ├── .gd файлы → GDScript парсер (Godot)
    │
    └── Генерирует одинаковый feature-map.yaml
```

### Этапы добавления языков:

1. **MVP:** TypeScript/JavaScript (веб-проекты)
2. **Потом:** C# (Unity)
3. **По запросам:** Остальные

---

## 💰 Бизнес-модель

### Freemium:

| План | Цена | Что включено |
|------|------|--------------|
| **Free** | $0 | Карта, автообновление, до 20 фич |
| **Pro** | $15/мес | AI в вебе, безлимит фич, приоритетная поддержка |
| **Team** | $10/чел | Общий веб-доступ, история, коллаборация |

### Математика до $500/мес:

- 50 юзеров по $10 = $500
- или 25 юзеров по $20 = $500
- или 5 команд по 10 человек = $500

Это **реалистичная цель** на 6-12 месяцев.

---

## ⚠️ Риски и как их обойти

| Риск | Уровень | Как обходим |
|------|---------|-------------|
| AI галлюцинирует | Средний | Человек проверяет, AI только предлагает |
| Люди не будут заполнять | Высокий | **Автотрекинг без участия человека** |
| Cursor/другие скопируют | Средний | Скорость, фокус на нише, комьюнити |
| Проекты слишком большие | Средний | Анализ по папкам, инкрементально |
| Безопасность кода | Низкий | Всё локально, код никуда не уходит |

### Главный риск:
Не технический. А **"будут ли люди реально этим пользоваться"**. 

Решение: поговорить с людьми до того, как писать код.

---

## 🐍 Уроборос: проект как своё собственное демо

**Идея:** Разрабатывая FeatureMap, использовать FeatureMap для FeatureMap.

### Почему это круто:

1. **Лучшее демо** — "вот реальный проект, вот его карта, кликай"
2. **Dogfooding** — сам чувствуешь что неудобно
3. **Доверие** — "мы настолько верим в продукт, что сами на нём живём"
4. **Документация** — карта проекта = документация проекта

---

## 🚀 Следующие шаги

### Сейчас (эта неделя):
- [ ] Поговорить с 5-10 людьми из целевой аудитории
- [ ] Показать эту идею, спросить "заплатил бы?"
- [ ] Собрать фидбек

### Если подтвердится (следующие 2-4 недели):
- [ ] Сделать MCP сервер с базовыми инструментами
- [ ] File watcher для автообновления
- [ ] Парсинг TypeScript → feature-map.yaml
- [ ] Простой веб с React Flow
- [ ] Подключить свой же проект как демо

### Потом:
- [ ] AI-анализ для группировки фич
- [ ] Настройки в вебе
- [ ] Добавить C# (Unity)
- [ ] Платные фичи

---

## 📝 Вопросы для разговоров с людьми

**Не спрашивай:** "Тебе нравится идея?"

**Спрашивай:**
1. "Как сейчас у вас дизайнеры понимают что в проекте готово?"
2. "Сколько времени в неделю тратится на объяснения между отделами?"
3. "Если бы был инструмент, который показывает карту всех фич проекта, понятную всей команде — использовали бы?"
4. "Сколько бы заплатили — $10, $20, $50 в месяц?"

---

## 🔧 Технические детали (для справки)

### MCP сервер — это просто:

```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

const server = new McpServer({ name: "featuremap" });

server.tool("scan_project", async () => {
  // Парсинг файлов
  return { features: [...] };
});

server.listen();
```

Минимальный сервер — 50-100 строк кода.

### Парсинг кода — решённая задача:

- **TypeScript Compiler API** — для TS/JS
- **Tree-sitter** — универсальный парсер для любого языка
- **LSP** — то, что IDE используют

Не изобретаем парсер, используем готовое.

### Сложность реализации:

| Компонент | Сложность | Время |
|-----------|-----------|-------|
| MCP сервер с инструментами | Низкая | 1-2 дня |
| File watcher | Низкая | 1 день |
| TypeScript парсер | Средняя | 2-3 дня |
| Веб с React Flow | Средняя | 3-4 дня |
| **Итого MVP** | | **~2 недели** |

---

## 💭 Финальная мысль

> "Идея — это 1% успеха. Остальные 99% — сделать."

Технически всё реализуемо. Ниша пустая. Боль реальная. 

Осталось проверить с живыми людьми и начать делать.

---

*Документ создан по итогам мозгового штурма. Дата: декабрь 2024*
*Обновлено: добавлены детали про MCP, автотрекинг, архитектуру*
